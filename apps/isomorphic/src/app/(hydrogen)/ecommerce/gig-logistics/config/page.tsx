import GIGConfigClient from '../../gig-config/gig-config-client';

export const metadata = {
  title: 'GIG Logistics Configuration',
};

export default function GIGLogisticsConfigPage() {
  return <GIGConfigClient />;
}
